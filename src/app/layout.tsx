import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "XMEML Transform Parser - Extract Transforms from Premiere Timelines",
  description: "Extract every transform, speed change, and effect from Premiere Pro XMEML exports. The confidence check that saves hours during post-production turnovers.",
  keywords: ["XMEML", "Premiere Pro", "transform", "parser", "post-production", "conform", "turnover", "Resolve", "Baselight"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
